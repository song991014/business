info:
  name: Finalization Count
  type: http
  seq: 26

http:
  method: GET
  url: http://193.123.247.238:8091/api/counter/grouped?ObjectPendingFinalizationCount&objName=/medical-mon-svr/WAS-OCISERVER-01
  headers:
    - name: Authorization
      value: "{{bearer_token_0ra8}}"
  params:
    - name: ObjectPendingFinalizationCount
      value: ""
      type: query
    - name: objName
      value: /medical-mon-svr/WAS-OCISERVER-01
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
