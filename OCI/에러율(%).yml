info:
  name: 에러율(%)
  type: http
  seq: 32

http:
  method: GET
  url: http://193.123.247.238:8091/api/counter/errorRate?minutes=5&objName=/medical-mon-svr/WAS-OCISERVER-01
  headers:
    - name: Authorization
      value: "{{bearer_token_0hc2}}"
  params:
    - name: minutes
      value: "5"
      type: query
    - name: objName
      value: /medical-mon-svr/WAS-OCISERVER-01
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
