info:
  name: slow-sql
  type: http
  seq: 12

http:
  method: GET
  url: http://193.123.247.238:8091/api/xlog/slow-sql?minutes=5&limit=3
  headers:
    - name: Authorization
      value: "{{bearer_token_18t3}}"
  params:
    - name: minutes
      value: "5"
      type: query
    - name: limit
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
