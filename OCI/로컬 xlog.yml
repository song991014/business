info:
  name: 로컬 xlog
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8091/api/xlog?minutes=1
  params:
    - name: minutes
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
