info:
  name: 6. 회수 완료
  type: http
  seq: 6

http:
  method: POST
  url: "{{SD_BASE_URL}}/Order/Retrieve-Complete"
  headers:
    - name: Authorization
      value: Bearer  {{LEMON_JWT_TOKEN}}
    - name: KEY
      value: "{{KEY}}"
      description: 로지올에서 발행한 인증키
    - name: API_COM_GBN
      value: "{{API_COM_GBN}}"
      description: 로지올에서 발행한 API 업체(브랜드) 구분 코드
  body:
    type: json
    data: |-
      {
          "SD_ORDER_NO": 589987523
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
